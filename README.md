# IRIS-FLOWER-CLASSIFICATION
Two models, namely logistic regression and Random Forest, have been used to classify three types of iris flowers. The data has been collected from the built-in load_iris() from sklearn library.
